// This file will export all models
// We'll add specific models here later 

const Restaurant = require('./Restaurant');
const User = require('./User');
const Review = require('./Review');

module.exports = {
    Restaurant,
    User,
    Review
}; 